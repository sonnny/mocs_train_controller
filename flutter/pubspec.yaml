name: ble_univ
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.6.2

dependencies:
  device_info_plus: ^11.2.2
  flutter:
    sdk: flutter
  get: ^4.6.6
  permission_handler: ^11.3.1
  universal_ble: ^0.15.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
